README file

This code was tested on MS visual studio.
Before running this code, go into the terminal and install the necessary libraries through pip install
pip install matplotlib
pip install collections
pip install numpy
pip install pandas
